Design Skinner
Codex skill for design skinning: change colors by module by default, with optional radius and shadow updates.

What It Does
Updates shared colors first
Keeps layout, spacing, and text unchanged
Supports SVG, Figma styles, and code-based UI
How To Use
Use $design-skinner to change the Header module primary color to #FDC708 and leave everything else unchanged.
Structure
SKILL.md - core skill instructions
agents/ - UI metadata
references/ - workflow and mapping rules
scripts/ - SVG helpers and validation
assets/ - sample theme data


Design Skinner
这是一个给设计稿做“换肤”的 Codex skill，默认只改颜色；只有明确要求时才改圆角和阴影。

它能做什么
先改共享颜色
保持布局、间距、文案不变
支持 SVG、Figma 样式和代码型 UI
怎么用
Use $design-skinner to change the Header module primary color to #FDC708 and leave everything else unchanged.
目录说明
SKILL.md - skill 核心规则
agents/ - 界面元数据
references/ - 流程和映射规则
scripts/ - SVG 辅助和校验
assets/ - 示例主题数据
